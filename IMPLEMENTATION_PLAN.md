- [x] Lock the current sketch behavior with a no-new-dependency fixture/test harness before hardening edits. `scripts/contract-fixtures.mjs` now runs under `bash scripts/validate.sh` and actively covers valid `succeeded`/`blocked`/`failed`, missing result artifact, malformed result JSON, task path normalization, artifact path escape rejection before launch, prompt rendering, fallback result writing with validation detail, resume map hit/miss/update behavior, command/env reconstruction, and additional-property rejection. Note: stdout session discovery uses a shell fixture because nested Node child stdout is empty in this harness environment; real OMO stdout/session semantics still require F5 live evidence before claiming compatibility.
- [x] Complete F1 task intake and path normalization in the existing `sketches/openclaw-omo-wrapper.ts` entrypoint. `runOmoTask` now uses an equivalent strict local validator for canonical task-schema semantics, rejects additional properties, enforces object/array/string/min-length checks, keeps `inputs` required, materializes omitted `verify` as `[]` in the normalized staged task, checks declared input existence before OMO launch, and rejects normalized artifact paths outside `cwd`. Fixture coverage now includes invalid task shape, missing declared inputs before launch, explicit `verify` defaulting, additional-property rejection, deterministic staged path normalization, and artifact escape rejection. Verification: `bash scripts/validate.sh` passed after self-healing the validator's root `.git` false positive; live OMO CLI semantics remain F5-only evidence and are not claimed here.
- [x] Complete F2 prompt rendering and artifact bootstrap in the existing sketch. `scripts/contract-fixtures.mjs` now snapshot-tests the full bounded prompt contract, verifies staged prompt contents use the normalized task and declared artifact paths, proves handoff/result parent directories exist before worker launch, and asserts the exact `OPENCLAW_WORKER_*` env var names exported to OMO. Verification: `bash scripts/validate.sh` passed.
- [ ] Complete F3 OMO invocation and optional resume mapping only after evidence checks. Fixture-side F3 is now covered: command composition assumes `bunx oh-my-opencode run --json --directory <cwd> <prompt>`, `--session-id` is passed only for a matching task map entry, stdout/stderr/exit code are captured as diagnostics, timeout failures return fallback `failed` results, session map hit/miss/update behavior is tested, and `<taskId>.invocation.json` records command, args, cwd, task/prompt paths, session-map path, resume session, timeout, worker env names, and completion/failure diagnostics. Remaining blocker before checking F3 complete: verify real OMO support for `run --json`, `--session-id`, and session-id discoverability in the F5 live evidence path.
- [x] Complete F4 result loading, schema validation, and status preservation. `sketches/openclaw-omo-wrapper.ts` now accepts loaded results only after strict local result-schema-equivalent checks pass and the declared handoff artifact exists. Fixture coverage preserves valid `succeeded`/`blocked`/`failed` unchanged, rejects unknown/additional properties, rejects invalid optional result field types, proves missing/malformed/result-contract-invalid artifacts write fallback `failed` results with validation detail in `errors`, and keeps stdout/stderr diagnostic-only. Verification: `bash scripts/validate.sh` passed with 28 tests across 15 subtests.
- [x] Extend `scripts/validate.sh` from structural smoke check to contract validation. It now checks required files, rejects nested project `.git`, validates example `WorkerTask` and `WorkerResult` artifacts for all three statuses against the project schemas with a no-dependency schema walker, checks boundary phrases, rejects direct Sable imports under `sketches`/`src`, guards future provider-facing paths from worker lifecycle terms, and runs the fixture harness that asserts prompt/env contract expectations, fallback result writing, status preservation, and resume-map behavior. Verification: `bash scripts/validate.sh` passed with 28 tests across 15 subtests after self-healing provider lifecycle term matching to catch camel-cased identifiers.
- [ ] Add the durable live-validation evidence path required by F5 after F1-F4 fixture checks pass. `scripts/live-omo-validation.mjs` and `validation/live-omo/README.md` now define the project-local evidence path, clean toy sandbox, nested-`.git` refusal, copied staged artifact set, and `LIVE_VALIDATION.md` format; `bash scripts/validate.sh` runs the runner self-test. No live OMO proof exists yet: `bun`, `bunx`, and `oh-my-opencode` were absent from PATH in this iteration, so real OMO `run --json`, `--session-id`, and session-discovery semantics remain blockers and must be proven from a clean worktree before checking F5/F3 complete.
- [ ] Decide wrapper promotion only after fixture and live evidence are available. Keep implementation centered on `sketches/openclaw-omo-wrapper.ts` for now because it is the only confirmed wrapper entrypoint and the current `src/` tree has no implementation files. If later promoted out of `sketches/`, remove or replace the old sketch path in the same change so the repo never has duplicate wrapper entrypoints.
- [ ] Keep scope locked to the first `OpenClaw -> OMO -> Sable` lane. Do not add OMC, OMX, Sable redesign, generalized multi-lane abstractions, or provider-layer worker semantics while this prototype remains unproven. Boundary work is limited to mechanical checks proving Sable stays generic and untouched by worker lifecycle state.
- [ ] Do not create a new spec file yet. Existing `specs/openclaw-omo-bounded-worker-wrapper.md` plus `specs/openclaw-omo-bounded-worker-wrapper.hardening.md` cover the confirmed gaps. Create or update a spec only if live OMO CLI behavior contradicts the assumed `run --json` / `--session-id` contract, or if a real OpenClaw use case requires controlled artifact paths outside `cwd`.
- [ ] Before a future build-loop implementation loop starts, resolve the active planning gate intentionally. `.omx/state/skill-active-state.json` currently shows build-loop planning mode, but no `.omx/plans/prd-*.md` or `.omx/plans/test-spec-*.md` artifacts are present. If execution continues under that gate, create/review those planning artifacts before implementation-focused tools run; otherwise run the documented direct sequence after reviewing this plan.
